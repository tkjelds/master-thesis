[project]
name = "master-thesis"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "tkjelds", email = "tkjelds385@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
master-thesis = "master_thesis:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
